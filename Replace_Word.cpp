#include<bits/stdc++.h>
using namespace std;
int main(){

    string s;
    string s1;
    cin >> s >> s1;

    string assign;
    
    for (string::iterator it = s.begin(); it != s.end(); ++it) {
          for (string::iterator it = s1.begin(); it1 != s1.end(); ++it1) {
       
    }
    }


}